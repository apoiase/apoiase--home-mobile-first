# Scan código → realidade — apoiase--home-mobile-first (2026-07-18)

Fontes: `graphify-out/graph.json` (claims), `.understand-anything/knowledge-graph.json`,
`package.json`, `plasmic.json`, `.github/workflows/*.yml`, Steampipe/aws (profile
`sa_automation_ro`), gitleaks (árvore + histórico completo).
Legenda: ✅ confirmado · ⚠️ divergência · ❓ validar · ❌ não encontrado

## Compute (serverless.yml)

Não há `serverless.yml`. O código afirma (via `.github/workflows/plasmic.yml`
e `plasmic-push.yml`) que o deploy é **GitHub Pages** (branch `gh-pages`),
não AWS — validado abaixo.

| Código afirma | Realidade | Status |
|---|---|---|
| Deploy via `github-pages-deploy-action` pra branch `gh-pages` | Sem recurso AWS com nome do repo (ver seção AWS) | ✅ consistente — site não é servido por AWS |

## Filas e eventos

Nenhuma. `plasmic.yml` reage a `repository_dispatch` (webhook do Plasmic
Studio); `plasmic-push.yml` reage a `push` em `main` — ambos eventos nativos
do GitHub Actions, sem fila própria.

## Bancos (nosso escopo — verificado)

Nenhum. Site estático (Next.js export), sem persistência própria.

## Config e secrets

**⚠️ SEC9 — 10 API tokens do Plasmic em texto plano em `plasmic.json`**
(committados, presentes no HEAD atual, um por projeto sincronizado: home
mobile-first, website mobile-first, antd, plasmic-basic-components,
plasmic-cms, plasmic-embed-css, plasmic-nav, react-awesome-reveal,
react-parallax-tilt, react-scroll-parallax). Gitleaks confirma (regra
`generic-api-key`, 10 findings no HEAD + 1 finding histórico num chunk
`_next/static/` de um deploy antigo pro gh-pages, arquivo não mais
presente na árvore atual). Fingerprints registrados no `.gitleaksignore`
da raiz da task (path absoluto). **Não rotacionado nesta rodada** — são
tokens do Plasmic (usados pelo `plasmic sync`/CI oficial do Plasmic pra
puxar o design do Studio), não credencial AWS/Apoia.se; rotacionar sem
mapear todo lugar que consome esse token (workflow `plasmic.yml` recebe o
token via `client_payload` do dispatch, não lê direto do `plasmic.json`
neste repo) pode quebrar o pipeline de sync sem aviso. Ver PRC de Security.

## Terceiros (validar — sem acesso)

| Terceiro | Papel | Status |
|---|---|---|
| Plasmic Studio | Design visual + codegen; dispara `plasmic.yml` | ✅ uso confirmado no código (workflows + plasmic.json) |
| GitHub Pages | Hosting do site publicado | ✅ confirmado (`gh-pages` branch nos dois workflows) |

## Realidade AWS (Steampipe, profile `sa_automation_ro`)

| Recurso | Resultado |
|---|---|
| `aws_lambda_function` | ❌ nenhum resultado |
| `aws_s3_bucket` | ❌ nenhum resultado |

**0 recursos AWS confirmados** com o nome do repo — consistente com o
código: o site é publicado via GitHub Pages, não AWS.

## Divergências e ações

- **SEC9** (acima): tokens do Plasmic em texto plano em `plasmic.json`.
  Registrado; mapeamento de escopo e rotação ficam como follow-up — fora do
  escopo scan+PR desta rodada (diretriz: hardening de infra não é o foco).
- `package.json` com `name: "tmp-cpa"` — scaffold nunca renomeado; sem
  impacto funcional, só um sinal de setup incompleto.
