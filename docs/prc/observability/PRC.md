# PRC — Observability (apoiase--home-mobile-first)

Site estático publicado via GitHub Pages — sem logs/métricas de aplicação
próprios (nenhum backend). Observabilidade possível é a do próprio GitHub
Pages/Actions (status do workflow) e, no client, o que o Plasmic Studio
oferecer nativamente.

## Checklist

- [x] N/a logs/métricas de runtime de aplicação (site estático, sem backend)
- [ ] OBS1: nenhum alerta automatizado se o deploy do `gh-pages` falhar —
  depende de alguém checar a aba Actions manualmente
